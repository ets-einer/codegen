import React from 'react'

interface %FORM_NAME%FormData {
    %INPUTS_INTERFACE%
}

interface %FORM_NAME%FormProps {
    formObject: %FORM_NAME%FormData
    setFormObject: React.Dispatch<React.SetStateAction<%FORM_NAME%FormData>>
}


const %FORM_NAME%Form = ({formObject, setFormObject}: %FORM_NAME%FormProps  ) => {

    const handleChange = (e: any) => {
        e.preventDefault()
        setFormObject({ ...formObject, [e.target.name]: e.target.value })
    }

    const handleSubmit = () => {
        console.log(formObject)
    }

    return (
        <form onSubmit={handleSubmit}>
            %INPUTS_JSX%
            <button type="submit">Submit</button>
        </form>
    );
}

export default %FORM_NAME%Form;